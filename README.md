This is a React application for showing total pageViews, total users, Avg. duration spent by a user on the portfolio website along with the displaying the number of api hits.

This application communicates with the .NET service deployed on render.

The portfolio visits are tracked using google analytics and the .NET api securely fetches data from google api.